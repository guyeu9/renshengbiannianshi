分步“纯 AI 开发”实现方案（可执行清单）
下面按“你不会写代码、让 AI 一步步生成可运行工程”的方式拆解。每一步都以“可运行、可验证”为目标。

Phase 0：项目脚手架（1~2 天）
目标：Flutter 能跑、数据库能建、能插入/查询一条记录。

让 AI 生成：
Flutter 项目结构（feature-first）
Riverpod 基础模板
Drift + SQLite 初始化、迁移框架
你要验证：
安卓真机运行
插入一条 food 记录并查询显示
Phase 1：数据层与“万物关联”最小闭环（3~7 天）
目标：实现 6 大实体中的 2~3 个（如 food/joy/friend）+ link 表。

让 AI 生成：
表结构 + DAO + migrations
link 的增删查（按 entity 查关联）
验证：
发布美食时能关联朋友；详情页能跳转关联记录（PRD 关联展示）
人生编年史APP产品需求文档
Phase 2：记录发布与列表（瀑布流）+ 本地图片存储（5~10 天）
目标：可用的“记录-浏览-详情”体验。

让 AI 生成：
图片选择/压缩/缩略图
瀑布流列表、筛选（日期/标签/评分/地点等，PRD有多维筛选）
人生编年史APP产品需求文档
Phase 3：WebDAV 备份（全量 zip + 增量） （5~12 天）
目标：实现 PRD 的“wev网盘备份zip + 增量备份（json、图片）”
人生编年史APP产品需求文档

让 AI 生成：
change_log 记录机制（每次改动写日志）
打包：导出 JSON + 拷贝新增媒体 → zip → AES 加密 → WebDAV 上传
恢复：下载 → 解密 → 回放 change_log / 导入数据
验证：
两台安卓机能通过同一 WebDAV 做同步（至少单向恢复成功）
Phase 4：搜索（FTS5）+ 图片 OCR 入库（7~14 天）
目标：先把“可用的搜索”做出来（不做向量也能很强）。

让 AI 生成：
search_doc 生成规则（标题+标签+地点+朋友名+ocr_text）
FTS5 虚拟表 + 同步触发更新（或应用层维护）
OCR pipeline（拍照/选图后后台跑 OCR，写入 media.ocr_text）
验证：
输入“日料/张三/成都/2024年3月开心”等能快速命中 
人生编年史APP产品需求文档
Phase 5：年度报告（SQL 统计 + AI 文案） （10~20 天）
目标：做出 PRD 的年度报告 MVP 
人生编年史APP产品需求文档

让 AI 生成：
年度 stats 的 SQL 集合（每个模块一套查询）
year_item 生成器（限长、可追溯）
LLM 调用封装（重试、超时、限流、缓存）
Prompt（强约束：只能引用 stats、不得造数字、highlight 必须带 record_id）
验证：
同一年度重复生成：结果稳定、数字完全一致（因为数字来自 SQL）
报告能导出 PDF/长图并分享

## ai管理的遗漏关注点提醒

### 1. API Key 加密存储

建议使用 `flutter_secure_storage` 进行 API Key 的加密存储，避免明文存储在数据库中。

### 3. 流式响应支持

AI 史官对话功能需要流式响应以实现打字机效果，不同 API 类型的流式实现方式不同。

### 5. 多模态支持

部分模型支持图片输入（如 Gemini Vision、Claude Vision），后续可扩展支持。

### 7. 与现有功能的集成顺序

建议按以下顺序集成：

1. 先完成 AI 模型管理功能
2. 再接入 AI 史官对话功能
3. 最后接入年度报告生成和智能搜索功能
- AI 史官对话页可读取当前激活的对话服务商配置
- 年度报告生成可读取当前激活的对话服务商配置
- 智能搜索意图解析可读取当前激活的对话服务商配置
- Embedding 向量生成可读取当前激活的 Embedding 服务商配置

# 编年史生成产物分析
## 一、当前生成的产物 1. PDF 文件
- 生成方式 ：使用 dart_pdf (pdf package) 生成
- 当前布局结构 ：
  ```
  ┌─────────────────────────────┐
  │ 标题 (24px 加粗)            │
  ├─────────────────────────────┤
  │ 时间范围：2024.01.01-2024.12.31 │
  ├─────────────────────────────┤
  │ AI 总结                     │
  │ (纯文本拼接，无真正AI调用)   │
  ├─────────────────────────────┤
  │ 模块汇总                    │
  │ 美食：12 条                 │
  │ 代表内容：xxx、yyy、zzz     │
  │ 旅行：8 条                  │
  │ 代表内容：...               │
  │ ...                         │
  └─────────────────────────────┘
  ``` 2. EPUB 文件
- 生成方式 ：使用 epub package 生成
- 结构 ：简单的 HTML，包含标题、时间范围、AI总结、模块汇总
- 缺少 ：封面图片、目录结构、章节分隔
## 二、与设计文档的差距
根据 开发设计文档.md 中的 Step 5：本地渲染排版 规范，完整的 PDF 应该包含：

设计文档期望 当前实现 差距 封面页 （带年度关键词） ❌ 无 需新增 总览页 （数据可视化图表） ❌ 无 需新增 各模块章节 （AI文案 + 图片 + 图表） ⚠️ 仅有纯文本 需大幅改进 结语页 ❌ 无 需新增

## 三、当前实现的主要不足 1. PDF 布局简陋
- 没有封面设计
- 没有数据可视化（应该用 SQL 统计画图表）
- 没有本地图片展示
- 样式只有纯文本，视觉体验差 2. 数据收集不完整
```
// 当前只收集了：
- 记录数量 (count)
- 记录标题/名称 (highlights，仅取前3个)
- 没有收集图片
- 没有收集详细描述
- 没有收集位置信息
- 没有收集评分/价格等数值
``` 3. 没有真正的 AI 生成
```
// 当前实现 (_buildAiSummary)
String _buildAiSummary
(DateTimeRange range, 
List<ChronicleModuleSummary> 
summaries) {
  // 只是本地文本拼接，不是调 AI API
  final summaryLines = <String>[
    '时间范围：${...}',
    '共整理 $total 条记录，覆盖 $
    {summaries.length} 个模块。',
    // ...
  ];
  return summaryLines.join('\n');
}
```
设计文档要求的是 5 步流水线 ：

```
Step 1 → Step 2 → Step 3 → Step 4 → 
Step 5
本地SQL    本地SQL   分模块   汇总调
AI  本地渲染
算统计指标  提取模块  调AI生成  生成总报
告 排版PDF
           素材      文案
```
当前只实现了 Step 1 的统计 + Step 5 的简化渲染 ，缺少 Step 3 和 Step 4 的 AI 调用。

### 四、后续改进建议 1. 短期改进（增强当前实现）
- 添加 PDF 封面页设计
- 添加数据可视化图表（各模块记录数量用柱状图/饼图展示）
- 收集并展示记录中的图片缩略图
- 优化 EPUB 目录结构 2. 中期改进（实现设计文档规范）
- 实现 Step 3 ：分模块调 AI 生成文案
- 实现 Step 4 ：汇总调 AI 生成最终报告
- 添加各模块详细章节页面
- 添加结语页 3. 长期改进（完善功能）
- 美食模块：年度味觉地图（城市分布 + 评分统计）
- 旅行模块：年度足迹地图
- 目标模块：年度目标完成率图表
- 羁绊模块：年度友谊报告
- 小确幸模块：年度情绪分布图


##  备份导出功能的遗漏
Excel导出功能完全缺失
位置 ： data_management_page.dart:616-625

```
_buildExportItem(
  isDark,
  textMain,
  textMuted,
  Icons.picture_as_pdf,
  Colors.red,
  'Excel / PDF 导出',  // 标题显示
  Excel/PDF
  '选择模块，适合阅读与打印',
  null,  // ❌ onTap为null，未实现
),
```
- 按钮存在但点击无反应
- 没有实现Excel生成功能 4. PDF导出功能完全缺失
- 与Excel导出共用同一个占位按钮
- 依赖包已安装： pdf: ^3.8.4 ，但未使用
### 🟡 中优先级缺失 5. 本地快照回滚功能为空实现
位置 ： backup_service.dart:899-900

```
Future<void> _restoreFromSnapshot() 
async {
  // ❌ 空实现！恢复失败时无法回滚
}
``` 6. 备份文件管理功能缺失
- 无法查看本地备份文件列表
- 无法删除旧备份文件
- 无法设置保留策略（如只保留最近10个备份）
- 无法将备份文件分享到外部（微信、邮件等）
### 🟢 低优先级缺失 7. 视频/音频文件处理
- 目前媒体文件收集 不区分文件类型 ，会收集所有文件
- 但没有专门针对视频/音频的处理 8. 备份压缩率优化
- 媒体文件未进行压缩处理（直接原文件打包）
## 三、🔍 特殊注意点
### 1. 媒体文件收集范围过大
```
// backup_service.dart:257-270
Future<List<File>> 
collectAllMediaFiles() async {
  final mediaDir = await getMediaDir
  ();
  // ...遍历所有文件...
  await for (final entity in 
  mediaDir.list(recursive: true)) {
    if (entity is File) {
      files.add(entity);  // ⚠️ 添加
      所有文件，不限于图片
    }
  }
  return files;
}
```
潜在问题 ：

- 如果媒体目录中有非图片文件（如临时文件、缓存文件），也会被备份
- 建议添加文件类型过滤


旅行/相遇模块没有删除功能，方案：

1. 使用 db.transaction() 包装所有数据库操作
2. 使用硬删除（物理删除）
3. 添加删除结果验证和日志，